# 2-bit-combinational-lock 
Hardware 2-bit combinational lock project using NOR, XOR, and NOT gates. Preset code 00: green LED ON when correct, red LED + buzzer if wrong. Entirely combinational logic on breadboard; no microcontroller. Includes demo video and circuit images.
